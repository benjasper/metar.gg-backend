// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"metar.gg/ent/region"
)

// Region is the model entity for the Region schema.
type Region struct {
	config `json:"-"`
	// ID of the ent.
	// The unique identifier of the record.
	ID uuid.UUID `json:"id,omitempty"`
	// The unique identifier of the import.
	ImportID int `json:"import_id,omitempty"`
	// Hash holds the value of the "hash" field.
	Hash string `json:"hash,omitempty"`
	// ImportFlag holds the value of the "import_flag" field.
	ImportFlag bool `json:"import_flag,omitempty"`
	// The last time the record was updated/created.
	LastUpdated time.Time `json:"last_updated,omitempty"`
	// local_code prefixed with the country code to make a globally-unique identifier.
	Code string `json:"code,omitempty"`
	// The local code for the administrative subdivision. Whenever possible, these are official ISO 3166:2, at the highest level available, but in some cases OurAirports has to use unofficial codes. There is also a pseudo code "U-A" for each country, which means that the airport has not yet been assigned to a region (or perhaps can't be, as in the case of a deep-sea oil platform).
	LocalCode string `json:"local_code,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// The wikipedia link of the region.
	WikipediaLink string `json:"wikipedia_link,omitempty"`
	// Keywords that can be used to search for the region.
	Keywords []string `json:"keywords,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the RegionQuery when eager-loading is set.
	Edges RegionEdges `json:"edges"`
}

// RegionEdges holds the relations/edges for other nodes in the graph.
type RegionEdges struct {
	// Airports holds the value of the airports edge.
	Airports []*Airport `json:"airports,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool

	namedAirports map[string][]*Airport
}

// AirportsOrErr returns the Airports value or an error if the edge
// was not loaded in eager-loading.
func (e RegionEdges) AirportsOrErr() ([]*Airport, error) {
	if e.loadedTypes[0] {
		return e.Airports, nil
	}
	return nil, &NotLoadedError{edge: "airports"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Region) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case region.FieldKeywords:
			values[i] = new([]byte)
		case region.FieldImportFlag:
			values[i] = new(sql.NullBool)
		case region.FieldImportID:
			values[i] = new(sql.NullInt64)
		case region.FieldHash, region.FieldCode, region.FieldLocalCode, region.FieldName, region.FieldWikipediaLink:
			values[i] = new(sql.NullString)
		case region.FieldLastUpdated:
			values[i] = new(sql.NullTime)
		case region.FieldID:
			values[i] = new(uuid.UUID)
		default:
			return nil, fmt.Errorf("unexpected column %q for type Region", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Region fields.
func (r *Region) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case region.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				r.ID = *value
			}
		case region.FieldImportID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field import_id", values[i])
			} else if value.Valid {
				r.ImportID = int(value.Int64)
			}
		case region.FieldHash:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field hash", values[i])
			} else if value.Valid {
				r.Hash = value.String
			}
		case region.FieldImportFlag:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field import_flag", values[i])
			} else if value.Valid {
				r.ImportFlag = value.Bool
			}
		case region.FieldLastUpdated:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_updated", values[i])
			} else if value.Valid {
				r.LastUpdated = value.Time
			}
		case region.FieldCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field code", values[i])
			} else if value.Valid {
				r.Code = value.String
			}
		case region.FieldLocalCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field local_code", values[i])
			} else if value.Valid {
				r.LocalCode = value.String
			}
		case region.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				r.Name = value.String
			}
		case region.FieldWikipediaLink:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field wikipedia_link", values[i])
			} else if value.Valid {
				r.WikipediaLink = value.String
			}
		case region.FieldKeywords:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field keywords", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &r.Keywords); err != nil {
					return fmt.Errorf("unmarshal field keywords: %w", err)
				}
			}
		}
	}
	return nil
}

// QueryAirports queries the "airports" edge of the Region entity.
func (r *Region) QueryAirports() *AirportQuery {
	return NewRegionClient(r.config).QueryAirports(r)
}

// Update returns a builder for updating this Region.
// Note that you need to call Region.Unwrap() before calling this method if this Region
// was returned from a transaction, and the transaction was committed or rolled back.
func (r *Region) Update() *RegionUpdateOne {
	return NewRegionClient(r.config).UpdateOne(r)
}

// Unwrap unwraps the Region entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (r *Region) Unwrap() *Region {
	_tx, ok := r.config.driver.(*txDriver)
	if !ok {
		panic("ent: Region is not a transactional entity")
	}
	r.config.driver = _tx.drv
	return r
}

// String implements the fmt.Stringer.
func (r *Region) String() string {
	var builder strings.Builder
	builder.WriteString("Region(")
	builder.WriteString(fmt.Sprintf("id=%v, ", r.ID))
	builder.WriteString("import_id=")
	builder.WriteString(fmt.Sprintf("%v", r.ImportID))
	builder.WriteString(", ")
	builder.WriteString("hash=")
	builder.WriteString(r.Hash)
	builder.WriteString(", ")
	builder.WriteString("import_flag=")
	builder.WriteString(fmt.Sprintf("%v", r.ImportFlag))
	builder.WriteString(", ")
	builder.WriteString("last_updated=")
	builder.WriteString(r.LastUpdated.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("code=")
	builder.WriteString(r.Code)
	builder.WriteString(", ")
	builder.WriteString("local_code=")
	builder.WriteString(r.LocalCode)
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(r.Name)
	builder.WriteString(", ")
	builder.WriteString("wikipedia_link=")
	builder.WriteString(r.WikipediaLink)
	builder.WriteString(", ")
	builder.WriteString("keywords=")
	builder.WriteString(fmt.Sprintf("%v", r.Keywords))
	builder.WriteByte(')')
	return builder.String()
}

// NamedAirports returns the Airports named value or an error if the edge was not
// loaded in eager-loading with this name.
func (r *Region) NamedAirports(name string) ([]*Airport, error) {
	if r.Edges.namedAirports == nil {
		return nil, &NotLoadedError{edge: name}
	}
	nodes, ok := r.Edges.namedAirports[name]
	if !ok {
		return nil, &NotLoadedError{edge: name}
	}
	return nodes, nil
}

func (r *Region) appendNamedAirports(name string, edges ...*Airport) {
	if r.Edges.namedAirports == nil {
		r.Edges.namedAirports = make(map[string][]*Airport)
	}
	if len(edges) == 0 {
		r.Edges.namedAirports[name] = []*Airport{}
	} else {
		r.Edges.namedAirports[name] = append(r.Edges.namedAirports[name], edges...)
	}
}

// Regions is a parsable slice of Region.
type Regions []*Region
