// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"metar.gg/ent/metar"
	"metar.gg/ent/predicate"
	"metar.gg/ent/skycondition"
	"metar.gg/ent/weatherstation"
)

// MetarQuery is the builder for querying Metar entities.
type MetarQuery struct {
	config
	ctx                    *QueryContext
	order                  []metar.OrderOption
	inters                 []Interceptor
	predicates             []predicate.Metar
	withStation            *WeatherStationQuery
	withSkyConditions      *SkyConditionQuery
	withFKs                bool
	loadTotal              []func(context.Context, []*Metar) error
	modifiers              []func(*sql.Selector)
	withNamedSkyConditions map[string]*SkyConditionQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MetarQuery builder.
func (mq *MetarQuery) Where(ps ...predicate.Metar) *MetarQuery {
	mq.predicates = append(mq.predicates, ps...)
	return mq
}

// Limit the number of records to be returned by this query.
func (mq *MetarQuery) Limit(limit int) *MetarQuery {
	mq.ctx.Limit = &limit
	return mq
}

// Offset to start from.
func (mq *MetarQuery) Offset(offset int) *MetarQuery {
	mq.ctx.Offset = &offset
	return mq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mq *MetarQuery) Unique(unique bool) *MetarQuery {
	mq.ctx.Unique = &unique
	return mq
}

// Order specifies how the records should be ordered.
func (mq *MetarQuery) Order(o ...metar.OrderOption) *MetarQuery {
	mq.order = append(mq.order, o...)
	return mq
}

// QueryStation chains the current query on the "station" edge.
func (mq *MetarQuery) QueryStation() *WeatherStationQuery {
	query := (&WeatherStationClient{config: mq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := mq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := mq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(metar.Table, metar.FieldID, selector),
			sqlgraph.To(weatherstation.Table, weatherstation.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, metar.StationTable, metar.StationColumn),
		)
		fromU = sqlgraph.SetNeighbors(mq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySkyConditions chains the current query on the "sky_conditions" edge.
func (mq *MetarQuery) QuerySkyConditions() *SkyConditionQuery {
	query := (&SkyConditionClient{config: mq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := mq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := mq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(metar.Table, metar.FieldID, selector),
			sqlgraph.To(skycondition.Table, skycondition.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, metar.SkyConditionsTable, metar.SkyConditionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(mq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Metar entity from the query.
// Returns a *NotFoundError when no Metar was found.
func (mq *MetarQuery) First(ctx context.Context) (*Metar, error) {
	nodes, err := mq.Limit(1).All(setContextOp(ctx, mq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{metar.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mq *MetarQuery) FirstX(ctx context.Context) *Metar {
	node, err := mq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Metar ID from the query.
// Returns a *NotFoundError when no Metar ID was found.
func (mq *MetarQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = mq.Limit(1).IDs(setContextOp(ctx, mq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{metar.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mq *MetarQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := mq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Metar entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Metar entity is found.
// Returns a *NotFoundError when no Metar entities are found.
func (mq *MetarQuery) Only(ctx context.Context) (*Metar, error) {
	nodes, err := mq.Limit(2).All(setContextOp(ctx, mq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{metar.Label}
	default:
		return nil, &NotSingularError{metar.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mq *MetarQuery) OnlyX(ctx context.Context) *Metar {
	node, err := mq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Metar ID in the query.
// Returns a *NotSingularError when more than one Metar ID is found.
// Returns a *NotFoundError when no entities are found.
func (mq *MetarQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = mq.Limit(2).IDs(setContextOp(ctx, mq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{metar.Label}
	default:
		err = &NotSingularError{metar.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mq *MetarQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := mq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Metars.
func (mq *MetarQuery) All(ctx context.Context) ([]*Metar, error) {
	ctx = setContextOp(ctx, mq.ctx, "All")
	if err := mq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Metar, *MetarQuery]()
	return withInterceptors[[]*Metar](ctx, mq, qr, mq.inters)
}

// AllX is like All, but panics if an error occurs.
func (mq *MetarQuery) AllX(ctx context.Context) []*Metar {
	nodes, err := mq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Metar IDs.
func (mq *MetarQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if mq.ctx.Unique == nil && mq.path != nil {
		mq.Unique(true)
	}
	ctx = setContextOp(ctx, mq.ctx, "IDs")
	if err = mq.Select(metar.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mq *MetarQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := mq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mq *MetarQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, mq.ctx, "Count")
	if err := mq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, mq, querierCount[*MetarQuery](), mq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (mq *MetarQuery) CountX(ctx context.Context) int {
	count, err := mq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mq *MetarQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, mq.ctx, "Exist")
	switch _, err := mq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (mq *MetarQuery) ExistX(ctx context.Context) bool {
	exist, err := mq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MetarQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mq *MetarQuery) Clone() *MetarQuery {
	if mq == nil {
		return nil
	}
	return &MetarQuery{
		config:            mq.config,
		ctx:               mq.ctx.Clone(),
		order:             append([]metar.OrderOption{}, mq.order...),
		inters:            append([]Interceptor{}, mq.inters...),
		predicates:        append([]predicate.Metar{}, mq.predicates...),
		withStation:       mq.withStation.Clone(),
		withSkyConditions: mq.withSkyConditions.Clone(),
		// clone intermediate query.
		sql:  mq.sql.Clone(),
		path: mq.path,
	}
}

// WithStation tells the query-builder to eager-load the nodes that are connected to
// the "station" edge. The optional arguments are used to configure the query builder of the edge.
func (mq *MetarQuery) WithStation(opts ...func(*WeatherStationQuery)) *MetarQuery {
	query := (&WeatherStationClient{config: mq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	mq.withStation = query
	return mq
}

// WithSkyConditions tells the query-builder to eager-load the nodes that are connected to
// the "sky_conditions" edge. The optional arguments are used to configure the query builder of the edge.
func (mq *MetarQuery) WithSkyConditions(opts ...func(*SkyConditionQuery)) *MetarQuery {
	query := (&SkyConditionClient{config: mq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	mq.withSkyConditions = query
	return mq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		RawText string `json:"raw_text,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Metar.Query().
//		GroupBy(metar.FieldRawText).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (mq *MetarQuery) GroupBy(field string, fields ...string) *MetarGroupBy {
	mq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MetarGroupBy{build: mq}
	grbuild.flds = &mq.ctx.Fields
	grbuild.label = metar.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		RawText string `json:"raw_text,omitempty"`
//	}
//
//	client.Metar.Query().
//		Select(metar.FieldRawText).
//		Scan(ctx, &v)
func (mq *MetarQuery) Select(fields ...string) *MetarSelect {
	mq.ctx.Fields = append(mq.ctx.Fields, fields...)
	sbuild := &MetarSelect{MetarQuery: mq}
	sbuild.label = metar.Label
	sbuild.flds, sbuild.scan = &mq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MetarSelect configured with the given aggregations.
func (mq *MetarQuery) Aggregate(fns ...AggregateFunc) *MetarSelect {
	return mq.Select().Aggregate(fns...)
}

func (mq *MetarQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range mq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, mq); err != nil {
				return err
			}
		}
	}
	for _, f := range mq.ctx.Fields {
		if !metar.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if mq.path != nil {
		prev, err := mq.path(ctx)
		if err != nil {
			return err
		}
		mq.sql = prev
	}
	return nil
}

func (mq *MetarQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Metar, error) {
	var (
		nodes       = []*Metar{}
		withFKs     = mq.withFKs
		_spec       = mq.querySpec()
		loadedTypes = [2]bool{
			mq.withStation != nil,
			mq.withSkyConditions != nil,
		}
	)
	if mq.withStation != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, metar.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Metar).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Metar{config: mq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(mq.modifiers) > 0 {
		_spec.Modifiers = mq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := mq.withStation; query != nil {
		if err := mq.loadStation(ctx, query, nodes, nil,
			func(n *Metar, e *WeatherStation) { n.Edges.Station = e }); err != nil {
			return nil, err
		}
	}
	if query := mq.withSkyConditions; query != nil {
		if err := mq.loadSkyConditions(ctx, query, nodes,
			func(n *Metar) { n.Edges.SkyConditions = []*SkyCondition{} },
			func(n *Metar, e *SkyCondition) { n.Edges.SkyConditions = append(n.Edges.SkyConditions, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range mq.withNamedSkyConditions {
		if err := mq.loadSkyConditions(ctx, query, nodes,
			func(n *Metar) { n.appendNamedSkyConditions(name) },
			func(n *Metar, e *SkyCondition) { n.appendNamedSkyConditions(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range mq.loadTotal {
		if err := mq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (mq *MetarQuery) loadStation(ctx context.Context, query *WeatherStationQuery, nodes []*Metar, init func(*Metar), assign func(*Metar, *WeatherStation)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*Metar)
	for i := range nodes {
		if nodes[i].weather_station_metars == nil {
			continue
		}
		fk := *nodes[i].weather_station_metars
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(weatherstation.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "weather_station_metars" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (mq *MetarQuery) loadSkyConditions(ctx context.Context, query *SkyConditionQuery, nodes []*Metar, init func(*Metar), assign func(*Metar, *SkyCondition)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Metar)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.SkyCondition(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(metar.SkyConditionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.metar_sky_conditions
		if fk == nil {
			return fmt.Errorf(`foreign-key "metar_sky_conditions" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "metar_sky_conditions" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (mq *MetarQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := mq.querySpec()
	if len(mq.modifiers) > 0 {
		_spec.Modifiers = mq.modifiers
	}
	_spec.Node.Columns = mq.ctx.Fields
	if len(mq.ctx.Fields) > 0 {
		_spec.Unique = mq.ctx.Unique != nil && *mq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, mq.driver, _spec)
}

func (mq *MetarQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(metar.Table, metar.Columns, sqlgraph.NewFieldSpec(metar.FieldID, field.TypeUUID))
	_spec.From = mq.sql
	if unique := mq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if mq.path != nil {
		_spec.Unique = true
	}
	if fields := mq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, metar.FieldID)
		for i := range fields {
			if fields[i] != metar.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := mq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mq *MetarQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mq.driver.Dialect())
	t1 := builder.Table(metar.Table)
	columns := mq.ctx.Fields
	if len(columns) == 0 {
		columns = metar.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mq.sql != nil {
		selector = mq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mq.ctx.Unique != nil && *mq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range mq.modifiers {
		m(selector)
	}
	for _, p := range mq.predicates {
		p(selector)
	}
	for _, p := range mq.order {
		p(selector)
	}
	if offset := mq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (mq *MetarQuery) Modify(modifiers ...func(s *sql.Selector)) *MetarSelect {
	mq.modifiers = append(mq.modifiers, modifiers...)
	return mq.Select()
}

// WithNamedSkyConditions tells the query-builder to eager-load the nodes that are connected to the "sky_conditions"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (mq *MetarQuery) WithNamedSkyConditions(name string, opts ...func(*SkyConditionQuery)) *MetarQuery {
	query := (&SkyConditionClient{config: mq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if mq.withNamedSkyConditions == nil {
		mq.withNamedSkyConditions = make(map[string]*SkyConditionQuery)
	}
	mq.withNamedSkyConditions[name] = query
	return mq
}

// MetarGroupBy is the group-by builder for Metar entities.
type MetarGroupBy struct {
	selector
	build *MetarQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mgb *MetarGroupBy) Aggregate(fns ...AggregateFunc) *MetarGroupBy {
	mgb.fns = append(mgb.fns, fns...)
	return mgb
}

// Scan applies the selector query and scans the result into the given value.
func (mgb *MetarGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mgb.build.ctx, "GroupBy")
	if err := mgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MetarQuery, *MetarGroupBy](ctx, mgb.build, mgb, mgb.build.inters, v)
}

func (mgb *MetarGroupBy) sqlScan(ctx context.Context, root *MetarQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(mgb.fns))
	for _, fn := range mgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*mgb.flds)+len(mgb.fns))
		for _, f := range *mgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*mgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MetarSelect is the builder for selecting fields of Metar entities.
type MetarSelect struct {
	*MetarQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ms *MetarSelect) Aggregate(fns ...AggregateFunc) *MetarSelect {
	ms.fns = append(ms.fns, fns...)
	return ms
}

// Scan applies the selector query and scans the result into the given value.
func (ms *MetarSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ms.ctx, "Select")
	if err := ms.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MetarQuery, *MetarSelect](ctx, ms.MetarQuery, ms, ms.inters, v)
}

func (ms *MetarSelect) sqlScan(ctx context.Context, root *MetarQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ms.fns))
	for _, fn := range ms.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ms.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ms.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ms *MetarSelect) Modify(modifiers ...func(s *sql.Selector)) *MetarSelect {
	ms.modifiers = append(ms.modifiers, modifiers...)
	return ms
}
